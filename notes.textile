h1. RubyProTips - Things that will make you a better Ruby coder. (Working title... )

h3. a |= [2,3,4,5,6]  Array union & intersection

h3. Using ||= for setting default settings for true/false

h3. Using .to_i to set default nil.to_i => 0

h3. ...

  def foo(bar = 1)
    bar
  end
  
  alternates: 
  def foo(args={})
    args[:bar] ||= 1
  end
  
  def foo(*args)
    bar = args[0] || 1
  end
  
  def foo(bar = nil)
    bar ||= 1
  end
  
h3. each_with_object

h3. map {|x| x.to_i } vs map(&:to_i)

h3. Array(...) like array wrap but core

h3. self.foo vs foo (public, protected, private)

h3. extend, include, module vs class  (ActiveSupport::Concern?)

h3. module include chains

h3. def foo(&block) vs def foo then using Proc.new.call and block_given?

h3. *_eval(&block) *_eval(&Proc.new) 

h3. foo and return... good until foo happens to be false/nil... try begin / end or tapping; its a gateway drug... 

i.e. session[:report] = nil and redirect_to([:reports, @challenge, @competition]) if params[:reset] == 'true'
